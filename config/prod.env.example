# =============================================================================
# MSS Product Configurator - Production Environment Configuration
# =============================================================================
# Copy this file to prod.env and fill in the placeholder values.
# Do NOT commit prod.env to version control.
#
# IMPORTANT: Production secrets should be stored in Azure Key Vault,
# not in environment files. This file is for reference and local tooling only.
# =============================================================================

# -----------------------------------------------------------------------------
# Azure Subscription & Resource Group
# -----------------------------------------------------------------------------
AZURE_SUBSCRIPTION_ID=<SUBSCRIPTION_ID>
AZURE_RESOURCE_GROUP=rg-msscfg-prod-westus2
AZURE_LOCATION=westus2
AZURE_ENVIRONMENT=prod

# -----------------------------------------------------------------------------
# Backend (FastAPI) Configuration
# -----------------------------------------------------------------------------

# PostgreSQL connection string (async driver)
# Server: psql-msscfg-prod-westus2.postgres.database.azure.com
# NOTE: In production, use Managed Identity or Key Vault reference instead of password
DATABASE_URL=postgresql+asyncpg://pgadmin:<PASSWORD>@psql-msscfg-prod-westus2.postgres.database.azure.com:5432/configurator

# Azure Storage Account
AZURE_STORAGE_ACCOUNT_NAME=stmsscfgprodwestus2
AZURE_STORAGE_BLOB_ENDPOINT=https://stmsscfgprodwestus2.blob.core.windows.net

# -----------------------------------------------------------------------------
# Frontend (Vite) Configuration
# -----------------------------------------------------------------------------

# Backend API URL (Container App FQDN)
# NOTE: In production, this may use a custom domain
VITE_API_URL=https://ca-msscfg-prod-westus2.azurecontainerapps.io

# -----------------------------------------------------------------------------
# Container Registry
# -----------------------------------------------------------------------------
# NOTE: Production may share registry with test or have dedicated registry
AZURE_CONTAINER_REGISTRY=acrmsscfgprodwestus2.azurecr.io

# -----------------------------------------------------------------------------
# Container Apps
# -----------------------------------------------------------------------------
AZURE_CONTAINER_APPS_ENVIRONMENT=cae-msscfg-prod-westus2
AZURE_CONTAINER_APP_NAME=ca-msscfg-prod-westus2

# -----------------------------------------------------------------------------
# Key Vault (for secret references)
# -----------------------------------------------------------------------------
AZURE_KEY_VAULT_NAME=kv-msscfg-prod-westus2

# Key Vault secret names (used by Container Apps for secret references):
# - database-password: PostgreSQL admin password
# - database-connection-string: Full PostgreSQL connection string
